<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name">
        <!--
            Exported because assist dispatch is a system-initiated launch: the platform
            starts this activity when the user triggers the assistant, and an unexported
            activity would never be reached. Export alone would let any installed app open
            a microphone session by firing ASSIST at us, so the launch is gated on a
            signature-level permission: the system uid that dispatches assist bypasses the
            component check, our own process is exempt as the same app, and no third-party
            app can hold the permission. getLaunchedFromUid() cannot do this job — the
            platform reports INVALID_UID unless the launching app opts into sharing its
            identity, which the system assist dispatch does not.
        -->
        <activity
            android:name=".AssistActivity"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!--
            The launcher entry is deliberately a separate component from the assist one.
            Any app can start a launcher activity, so it carries no permission, and it is
            what "open Mentat" resolves to when the car button is owned by another
            assistant (Android Auto routes the steering wheel button to Google's). The
            session it opens is as visible as any other: a foreground activity plus the
            microphone foreground-service notification.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".session.VoiceSessionService"
            android:exported="false"
            android:foregroundServiceType="microphone" />
    </application>
</manifest>
